import { FileCheck2, LoaderCircle, LockKeyhole, ShieldCheck, Sparkles } from 'lucide-react'
import { useState } from 'react'
import { api } from '../api'

export default function Login({onLogin}){
  const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false)
  const submit=async(e)=>{e.preventDefault();setLoading(true);setError('');try{onLogin(await api.auth.login(email,password))}catch(err){setError(err.message)}finally{setLoading(false)}}
  return <div className="grid min-h-screen bg-slate-950 lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden p-14 text-white lg:flex lg:flex-col"><div className="absolute -left-24 top-20 h-80 w-80 rounded-full bg-indigo-500/20 blur-3xl"/><div className="absolute bottom-0 right-0 h-96 w-96 rounded-full bg-violet-500/15 blur-3xl"/><div className="relative flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-white text-slate-950"><FileCheck2 size={21}/></span><div><p className="font-display text-xl font-black">ResumeForge</p><p className="text-[10px] font-bold uppercase tracking-[.24em] text-indigo-300">Private application studio</p></div></div><div className="relative my-auto max-w-xl"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-xs font-bold text-indigo-200"><Sparkles size={14}/> Tailored for the role. True to your story.</div><h1 className="font-display text-5xl font-black leading-[1.05] tracking-[-.04em]">Forge every application with intent.</h1><p className="mt-6 max-w-lg text-base leading-7 text-slate-400">Your private workspace for resume tailoring and personalized cover letters, built around your own templates.</p><div className="mt-10 grid grid-cols-2 gap-4"><Trust icon={ShieldCheck} title="Private by design" text="Only four approved accounts"/><Trust icon={LockKeyhole} title="Templates isolated" text="Every user has separate files"/></div></div><p className="relative text-xs text-slate-600">ResumeForge · Access restricted</p></section><section className="grid place-items-center bg-white p-6 sm:p-10"><div className="w-full max-w-md"><div className="mb-9 lg:hidden"><p className="font-display text-2xl font-black">ResumeForge</p></div><p className="text-xs font-bold uppercase tracking-[.18em] text-indigo-600">Private access</p><h2 className="mt-3 font-display text-4xl font-black tracking-tight text-slate-950">Welcome back.</h2><p className="mt-2 text-sm text-slate-400">Use the credentials provided by your administrator.</p><form onSubmit={submit} className="mt-9 space-y-5">{error&&<div className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-600">{error}</div>}<label><span className="label">Email address</span><input required type="email" autoComplete="username" className="input py-3.5" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@domain.com"/></label><label><span className="label">Password</span><input required type="password" minLength="6" autoComplete="current-password" className="input py-3.5" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••"/></label><button disabled={loading} className="primary-btn w-full py-3.5">{loading?<LoaderCircle className="animate-spin" size={18}/>:<LockKeyhole size={17}/>} {loading?'Signing in…':'Sign in to ResumeForge'}</button></form><p className="mt-7 text-center text-xs leading-5 text-slate-400">No public registration. Contact your administrator if you cannot access your account.</p></div></section></div>
}
function Trust({icon:Icon,title,text}){return <div className="rounded-2xl border border-white/10 bg-white/[.04] p-4"><Icon size={18} className="text-indigo-300"/><p className="mt-3 text-sm font-bold">{title}</p><p className="mt-1 text-xs text-slate-500">{text}</p></div>}
